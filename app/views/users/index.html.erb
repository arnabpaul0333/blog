<% @users.each do |user| %>
<p><%= user.first_name%>
  <%= user.email%>
  <%= image_tag user.photo.url(:thumb) %>
  <%= link_to "destroy user", user_path(user), method: :delete  %>
  <%= link_to "Add comments",new_user_comment_path(user) %>
</p>
<% end%>
